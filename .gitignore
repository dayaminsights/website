# Local-only working files — never deploy to GitHub Pages
index.wireframes-backup.html
mockups/

# Verification harness (tools/) and everything it writes (tmp/).
# tmp_* still catches any stray scratch file dropped at the root.
tools/
tmp/
tmp_*
node_modules/

# Audit paperwork — the repo is public and everything committed is served
docs/seo/
