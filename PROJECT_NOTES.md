# Dayam Insights — Landing Page Notes

Six static pages, no build step, GitHub Pages ready. Shared `assets/css/site.css` and `assets/js/site.js` (every feature in the script guards for its own elements, so one file serves every page). Targets Tier-2 India audience — plain language, no jargon.

## Pages (split 2026-09-17)
- `index.html` — a hub, not a brochure (rebuilt 2026-09-19): hero (three rows in the slogan’s order) → `#about` (what Dayam Insights is: statement + Built for / How we work / What you keep) → `#value` (See. Automate. Grow. — three link-cards, one per service page, each carrying the symptoms that page is for and a still from that page’s hero video) → `#signal` (the scroll argument, kept as the one thing that is not a menu) → `#cta`. Carries the ProfessionalService schema. The old `#problem`, `#dashboards`, `#automation`/`#ai` and `#websites` blocks are gone: every figure on them was a copy of its service page’s main block. The nav is five flat links (Websites / Dashboards / Automation & AI / How we work / FAQ) with no hash links, so the scroll-spy in `site.js` (`.nav-links a[href^="#"]`) has no targets on any page and is inert — harmless, but that is why the nav no longer underlines as you scroll the homepage.
- **Three service pages**, all built to one shape: hero (`.page-hero`) → `#problem` (four cards) → the "four things" section with that service’s figure → `#included` → `#process` → `#proof` → `#cta` (own form, intent pre-set) → `#more` (cross-sell strip, after the close). Each carries a `Service` + `BreadcrumbList` schema and its own `FACTS STILL TO SUPPLY` comment. Change the shape on one and change it on all three.
- `websites.html` — the standalone websites service page (added 2026-09-18). Hero → `#problem` (four ways a site loses the enquiry) → `#jobs` (the `.webx` four-jobs figure, same component as the homepage teaser) → `#included` → `#process` → `#proof` → `#more` (cross-sell to the other three services) → `#cta` with its own contact form. Carries a `Service` + `BreadcrumbList` schema, and its own `FACTS STILL TO SUPPLY` comment.
- `dashboards.html` — Dashboards & analytics (added 2026-09-18). `#questions` is the four-questions section, carrying the `.dash-mock` from the homepage. `#proof` reuses the retail case.
- `automation.html` — Workflow automation & AI (added 2026-09-18). Two service blocks, as the homepage pairs them: `#work-that` (four kinds of work, `.flow-mock`) and `#ai` (`.ai-mock`), so the footer’s "AI assistants" link is `automation.html#ai`. `#proof` carries both the distribution and services cases, so it has two `.res` articles where the others have one.
- `how-we-work.html` — the ladder (`#ladder`, the climb) and `#process`.
- `faq.html` — sixteen questions in five groups; carries the FAQPage schema. Group 02 is Websites, placed second on purpose so a website buyer reaches it before the operations groups.
- Inner pages end on a `#next` band linking to `./#contact` — except the three service pages, which carry their own `#contact` form (their footer "Talk to us" is therefore `#contact`, not `index.html#contact`). Nav and footer are duplicated per page (edit all six); the current page's nav link carries `aria-current="page"` and `.is-current`. Chapter numbers restart per page.
- Links back to the homepage name the file: `index.html`, `index.html#contact`. Never `./` or `./#x` — on a server `./` is the homepage, but opened from disk (`file://`, which is how pages get previewed here) it is the folder, and the browser shows a directory listing. Links to a service go to its page (`dashboards.html`), not the homepage teaser. The nav scroll-spy only runs for in-page hash links, so it is inert on inner pages.

## Design system (CSS vars in `:root`)
Logo DNA restyle (2026-09-16): every visual decision derives from the mark — the blue square is the signal, the i-bar is input, the D is the system.
- Ground `--bg:#F8FAFC`, bands `--bg-2`/`--surface:#FFFFFF`, `--surface-sunken:#F1F5F9`, `--placeholder:#DCE3EC`; navy panels `--panel:#07162D`, `--panel-2:#0D2240` (dashboard, flow, AI and signal mocks, CTA band, footer)
- Lines `--line:#E5EAF2`, `--line-strong:#CBD5E1`, `--line-invert`
- Ink `--ink:#07162D`, `--ink-2:#334155`, `--muted:#64748B` (15px and up only), `--muted-2:#475569` (labels under 15px); on panels `--on-panel:#F8FAFC`, `--on-panel-muted:#9FB0C8`
- Accent is the mark's blue: `--accent:#1E7BFF` for nodes, lines and markers; `--accent-hover:#1668E6` wherever blue sits under or behind text (5.05:1 with white). `--good` is that deeper blue and `--warn` is ink: the DNA has no green or amber. Use blue sparingly — one active element per composition.
- Font: Inter 400–800 for everything (`--font-ui`, `--font-body`, `--font-mono` all resolve to it); headings 700–800 tight-tracked; labels 11px caps .14em
- Shape: all radii 0 (`--r-*` are 0 and no literal radius remains); nodes, dots and markers are squares, never circles; icons get butt caps and mitre joins via `svg [stroke-linecap]`
- Elevation: hairlines, no resting shadows (`--shadow-sm/md:none`); cards lift 4px with `--lift` on hover only
- Layout: `--maxw:1360px`, `--gutter:clamp(24px,4vw,64px)`, `--maxw-text:720px`, `--maxw-narrow:520px`; sections `.pad` 72→128px
- Eyebrows read `02 ▪ LABEL`: chapter number, 5px blue square, label. The hero eyebrow is set like the tagline under the mark.
- Hero (rebuilt 2026-09-17): headline, one sentence, one button + one link on the left; on the right `.hero-system`, the four things we build as one framed panel of rows (Data · Automation · AI · Websites, each a link to Solutions) on the blueprint grid; the audience line beneath. The blue `.hs-signal` walks the rows once on load (site.js `hsWalk`), rests on Data, and follows the pointer. The old pipeline cards, laptop dashboard and three service cards are gone. h1 is `clamp(34px,3.75vw,54px)` (stacked: sized to the column) so the longest authored line ("Your business shouldn’t", 11.04em) never wraps.
- Logo (2026-09-17): `assets/img/logo.svg` is the vector master, traced from the supplied bitmap (viewBox 283×297: blue 48px square, i stem, D ring with an inner crescent). `logo.png` / `logo-white.png` (1024px) are its renders for the nav and the navy footer; `touch-icon.png`, `favicon.svg` and `og-image.png` are built from the same mark. If the mark changes, edit the SVG and re-render the PNGs from it. The nav carries the mark as inline SVG (`.mark`) so it can sign the page on load: the blue square switches on, the D is drawn outward from its curve by a stroke mask (`#markRing`), both stems rise, the crescent unfolds — dot 0 · ring 100-620 · stems 500-880 · curl 760-1020. It plays on every page load; no JS and reduced motion both land on the finished mark. The mark keeps one size in the nav — it used to scale to .85 on scroll while the wordmark beside it did not, which pulled the lockup apart.

## Animation conventions
- `.reveal` + `.in` (toggled by IntersectionObserver) for scroll-triggered fade/slide-up. `data-d="1..4"` = stagger delay via `transition-delay`.
- Reveal variants for scroll rhythm: `.reveal-scale` (fade + slight scale-up, used on `#process` section heads), `.reveal-left`/`.reveal-right` (slide in from side, used on `.web-grid web-block` rows — direction matches whether copy or visual is on that side). Collapse to translateY on mobile (`@media max-width:1000px`).
- `prefers-reduced-motion: reduce` disables all animation globally (`*{animation:none!important}`) — **always add an exception when adding a new keyframe or a transform pre-state**, or the element is stranded in it. Pseudo-elements and SVG children are not covered by the class-based resets: `.rung::after`, `.proc-line i`, `.sy-dot` and `.sy-out` each need their own override. The hero entrance is the exception that needs none, because every pre-state lives only in a `from` keyframe with `fill-mode:both`, so dropping the animation leaves the natural state.
- Animated counters: `data-count`, `data-prefix`, `data-suffix`, `data-dec` attributes + `animateCount()` JS (rAF, cubic ease-out, 1400ms).
- "Build-up" pattern (`.bm-block`/`.dk`/`.flow-node` etc.): base `opacity:0;transform:translateY(Npx)` → parent `.web-visual.in` reveals with per-child `transition-delay`.
- **Looping pipelines**: `pulse(root, selector, stepMs, holdMs, restMs)` walks a `.lit` class along a diagram's children and loops, but only while that diagram is on screen (its own IntersectionObserver starts and stops it, and `mouseenter` replays a pass). Used by `.hero-pipe` (`.pipe-step`) and `.flow-mock` (`.flow-node,.fl-arrow` — nodes and connectors share the walk so the pulse travels the arrows too). Returns a no-op under `prefers-reduced-motion`.
- **Reading-order stagger**: `.points li`, `.uc`, `.res-lines > div` and `.tech-item` start at `opacity:0` and are revealed by `.in` on their reveal parent with 180–480ms `nth-child` delays, so a heading lands before its supporting rows.
- Ladder rungs stagger via an inline `--i` custom property (`transition-delay:calc(var(--i) * 55ms)`) rather than `data-d`, because there are six of them plus the handoff row and `data-d` only defines 1–4. Tool chips stagger on `.rung.open` with `nth-of-type` delays.
- `.proc-line` carries `.reveal` but overrides it to draw with `scaleX(0) → scaleX(1)`; `.proc-line.reveal` beats `.reveal` on specificity.
- SVG icons: `viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"`.
- JS reveal selector is `.reveal, .reveal-scale, .reveal-left, .reveal-right, .web-visual` — any new animated visual block should get one of these classes to be observed.
- Scroll progress bar: `#scrollBar` (inside `.scroll-progress`, fixed top of page) — width set as % of scroll in the rAF-batched scroll handler.
- **Hero entrance** is the one sequence on the page driven by CSS animation on load rather than by the observer, because the hero is above the fold and an observer that fires immediately is just a slower way to do the same thing. Keyframes `heroGround` / `heroLine` / `heroRise` / `heroPanel`; timeline is ground 0–700ms, eyebrow 200, the three masked `h1 .ln` lines 380/470/560, subhead 800, CTA 960, trust 1080, pillar panel 1200. The hero visual's own `.in` delays start at 860ms so the composition assembles left to right rather than both halves racing.
- **Masked line reveal**: `<span class="ln"><span>text</span></span>` with `overflow:hidden` on the outer and `translateY` on the inner. `overflow:hidden` on an auto-height block clips the pre-state without ever clipping a line that *wraps* — the block just grows — so this degrades safely at narrow widths. Used on the hero h1 and the final CTA h2, nowhere else.
- **Scroll-linked, not just revealed**: `#signal` (scrubbed continuously from scroll position), `#process` (`--pp` fill + done/active states), and the ladder climb rule. Everything else on the page is still a one-shot fade.
- The process row is driven from the **section's own scroll travel**, not per-step observers: all four steps are side by side, so they enter the viewport in the same frame and a per-step observer marks the row finished before it has been read. `measureProcess()` caches the offset; `updateProcess()` runs inside the existing rAF handler and never reads layout.

## Page structure (section order, top to bottom)
1. `#top` — Hero. Copy column (eyebrow → "Your business shouldn't need you to run every little thing." → what we build → CTA pair → audience line + sector row) beside `.hero-stage` (`.hero-pipe` 5-step pipeline: Order in → Automation → Stock & invoice → Dashboard → Decision, sitting over the `.lap` laptop). `.hero-services` is the three-pillar strip (See / Automate / Grow) linking to `#dashboards` / `#automation` / `#websites`
2. `#positioning` — `.stmt-band`: the one-sentence positioning statement + supporting line, hairline rules above and below
3. `#problem` (band) — four numbered pain cards: manual work, disconnected data, slow decisions, growth bottlenecks
4. `#ladder` — the handoff ladder. Rung 01 Create is tagged **Yours · done**, then a `.handoff` band, then rungs 02–06 (Innovate, Integrate, Automate, Accelerate, Elevate) tagged **Ours**, each opening to symptom / what we do / what changes / tools. Static HTML rungs + one delegated click handler
5. `#signal` (band, `.scrolly`) — the signature scroll section. See its own heading below
6. `#dashboards` (`.web-show`) — SEE. One block, `.dash-mock`
7. `#automation` (`.web-show`) — AUTOMATE. Two blocks: workflow automation (`.flow-mock`, five nodes: WhatsApp → Order → Invoice → Payment → Dashboard) and `#ai` (`.ai-mock` assistant panel: question → sources it reads → answer)
8. `#websites` (`.web-show band`) — GROW. One block: `.browser-mock` + `.search-mock` (a visitor journey, not a search-ranking claim)
9. mid-page CTA
10. `#work` — three example projects as Problem / Solution / Outcome (`.res-lines`), each tagged "Example project"
11. `#process` (band) — **"How an engagement runs"**: Discover → Prioritise → Build → Improve. Retitled so it and the ladder never both claim "how we work" — the ladder is the arc across years, this is what happens after you sign
12. `#faq` — 13 questions, mirrored in the FAQPage schema
13. `#cta` (band) — contact form + WhatsApp route
14. Footer

**`#about` (founder) is currently removed** — pulled on request while the founder facts do not exist. The rewritten markup and its CSS are parked in `docs/removed-founder-section.html` with restore instructions at the top; the nav link was taken out of `.nav-links`, `.mm-links` and the footer "Company" column. Its chapter number was `09`, the last one, so nothing needed renumbering. Note the page lost its "why trust us" section: proof now rests on the sector row, the ladder, the example projects and the FAQ.

Section heads carry a chapter number via `data-ch` on `.eyebrow` (01 problem → 08 process). FAQ and CTA are deliberately unnumbered: they are the close, not part of the argument.

## `#signal` — the one scroll interaction that argues something
Eighteen `<circle class="sy-dot">` elements each hold up to three position sets as inline custom properties: `--sx/--sy` scattered, `--gx/--gy` tabulated (3 rows × 6), and on the seven tagged `.key`, `--cx/--cy` plotted onto a weekday series. The geometry is authored into the markup, so it is static, indexable and needs no JS to compute.

**The visual is scrubbed, not stepped.** JS writes five phase numbers on the section each frame and CSS interpolates everything from them in `calc()`. Pause halfway through the section and the dots sit halfway. Scroll is never intercepted or retimed — the page scrolls exactly as far as the wheel says and only the drawing reacts, which is the line between scroll-*linked* and scroll-*jacking*.

| var | drives |
|---|---|
| `--t1` | scattered → tabulated (all 18 dots) |
| `--t2` | tabulated → plotted (the 7 `.key` dots); the other 11 fade to a field |
| `--t3` | the series line (`stroke-dashoffset: calc(1 - var(--t3))`) and the axis |
| `--t4` | area fill and the peak marker |
| `--t5` | the two `.sy-out` action rows |

Dot position composes the two moves rather than replacing them — `--lx/--ly` lerp scatter→grid, and `.sy-dot.key` lerps from `var(--lx)` on to `--cx/--cy`, so `--t2` continues from wherever `--t1` left the point instead of snapping. **Nothing in the scrubbed set has a CSS `transition`**: the reader's scroll is the timeline, so a transition only adds lag between the wheel and the picture. The one exception is `fill`, which steps at a threshold and does need easing.

`updateSignal()` maps scroll to progress `p` (0 = step 1 centred, 1 = step 5 centred) and runs each phase through a **smoothstep** (`seg()`), so phases ease in and out instead of starting and stopping on a hard edge. The windows deliberately **overlap** — the line starts drawing at `p=.42` while points are still landing until `p=.52` — which is what makes it read as watching something happen rather than watching five slides. `data-stage` is still written, but now only feeds the `.sy-state` readout text, the dot fill colour and the lit step.

Runs inside the existing rAF handler; `measureSignal()` caches the offsets so the loop never reads layout. Below 901px and under `prefers-reduced-motion`, `syStatic()` pins every phase to 1 (the finished, readable frame) and CSS drops the sticky pin. The CSS defaults are also `1`, and a `<noscript>` block releases the rest of the page's reveals, so the no-JS path gets the finished frame rather than an empty panel. This is the only sticky section on the page.

`#tech` was removed: its tool lists now live in the ladder rung that uses them, and its trust line survives as `.ladder-runs` (cloud accounts you own · backed up daily · you keep the logins).

The SEE/AUTOMATE/GROW pillar section was removed: its three `<h3>`s were the same sentences the three sections below use as their `<h2>`s. `.hero-services` is now the only pillar strip, and its cards name the capability ("Dashboards & analytics") while the sections below carry the promise. Nav "Solutions" points at `#dashboards`.

Nav is deliberately short: Solutions / Work / How it works / About / FAQ, plus "Find your bottleneck". It collapses to the menu button at 900px (not 760px) because brand + five links + two buttons measured 845px against a 768px viewport.

## Visual rhythm
- `.web-show` sections (`#growth`, `#intelligence`) get a full-bleed subtle panel background (`::before`, 100vw breakout) to visually separate the "showcase" sections from the plainer text sections. Adjacent `.web-show` sections share one continuous panel (no double border) via `.web-show + .web-show` / `:has()` selectors.

## Key shared classes for the "showcase" sections (#growth, #intelligence)
- `.web-show` — section wrapper, `padding-top:0`
- `.web-block` — one row (margin-bottom spacing between rows)
- `.web-grid` — 2-col grid (`1fr 1.1fr`), `.web-grid.rev` reverses order (visual left, copy right) via `order`
- `.web-copy` — left text column; `.title.sm` for h3 sub-headings, `.web-stats` for stat rows
- `.web-visual` — right animated visual column; gets `.in` class on scroll-into-view
- Responsive: `@media(max-width:1000px)` collapses `.web-grid` to 1 col (and resets `.rev` order); `@media(max-width:760px)` has further mobile tweaks (bm-cards, funnel, dash-kpis, flow-row)

## Recent work (most recent first)
- **Homepage rebuilt as a hub (2026-09-19).** Owner’s brief: say what Dayam Insights is, where it adds value, then send each reader to their page. Before: 10,217px with three service blocks (4,086px) whose figures were identical to the service pages and whose copy was 10–60% verbatim. After: 6,994px — hero 630 · about 659 · value 1,071 · signal 2,732 · form 1,262.
  - **`#about`** is assembled only from lines the site already committed to (schema description and audience, FAQ promises, footer blurb, how-we-work): a statement beside three facts — Built for / How we work / What you keep. No founder facts exist, so none are implied. `.about-grid` / `.about-facts` are new, small, on the site’s tokens.
  - **`#value`** uses the slogan **See. Automate. Grow.** (it lived only in the schema until now) as the spine: three `.vcard` link-cards in that order, each with a still from its page’s hero video (`assets/video/<page>-card.jpg`, the *after* wide shot — the poster frames were all the phone close-up and looked alike), one line on the service, three symptom lines, and a link. The whole card is the link; hover lifts on `translate` like every other card.
  - **Sitewide order now follows the slogan**: nav is Dashboards · Automation & AI · Websites · How we work · FAQ on all six pages (was Websites-first, a funnel choice of mine, not the owner’s), hero rows are See / Automate / Grow, footer Solutions already matched. `tmp_nav.js` had still carried the deleted Work link and re-inserted it on a rerun — fixed in the generator.
  - `#signal` stays unchanged. `#problem` is gone: its four pains became the symptom lines on the value cards, so routing is one step.
  - Suites updated for the new shape: `tmp_funnel.js` tests the CTA-presets-intent behaviour on dashboards.html (the homepage no longer has service CTAs), `tmp_motion_test.js` no longer expects mocks on the homepage, `tmp_site.js` asserts the new nav order. All green on all six pages.
- **Motion language tuned to the cinematic-motion skill, within the site’s own system (2026-09-18).** The owner invoked the GSAP/Lenis skill; offered three scopes and chose "refine within the site’s own system" over adding GSAP + Lenis + cursor + magnetic (rejected as ~80KB of JS, scroll hijack against the site’s scroll-linked rule, and studio-portfolio atmosphere on a B2B SME site). What changed, all vanilla CSS:
  - **Tokens.** `--t-reveal` 480 → 850ms (the skill’s 0.75–1.1s band), new `--t-media:1100ms` and `--t-hover:350ms`, new `--ease-cine` = power4.out `cubic-bezier(.165,.84,.44,1)`. Reveals travel 24px (was 12), list items 16px. Headings stay static — that rule stands.
  - **Media clip-reveal.** Every `.web-visual` mock (dash, flow, AI, handover panels on all four pages that have them) now opens top-down behind `clip-path:inset()` over 1.1s while settling from scale 1.06 — the same move the hero video makes on load, so a page reads as one language. The clip sits on the mock (`.web-visual > *`), **not** on `.web-visual`: the IntersectionObserver that adds `.in` reads a clipped element as zero area and never fires. No-JS blocks on all six pages and the reduced-motion block clear it.
  - **A latent bug fixed.** `.prob,.res{transition:transform …,border-color,box-shadow}` (the hover rule) came later than `.reveal{transition:opacity,transform}` and replaced it, so problem cards and case cards had always popped in with no opacity transition. Now one transition list on `.reveal` carries opacity/transform (reveal) plus `translate`/border/shadow (hover); the hover lift uses `translate:0 -4px` so it never fights the reveal’s `transform`; `.reveal[data-d]` delays are per-property (`Xms,Xms,0s,0s,0s`) so the stagger never lags a hover.
  - **Hover.** Arrows glide 5px (6 on the hero rows) over 350ms on the cinematic ease; colour feedback stays on `--t-fast`.
  - Tested (`tmp_motion_test.js`): mock starts clipped, `.in` lands on scroll-in, clip fully open and scale 1 after the transition, card reveal 850ms with hover props at 200ms, stagger delays only the reveal, hover still lifts, reduced motion never clips or scales, no-JS shows every figure on all four pages. All other suites green; `tmp_anim.js` now waits 1.4s for reveals before measuring alignment.
- **Hero motion videos, all three service pages (2026-09-18).** A 30s silent 3:2 loop in each hero, beside the headline, telling the *before* the page never shows. All three share one rig (`motion/lib.js` + a copied camera/phone/timeline skeleton in each `motion/<page>.html`): a 1080×720 stage, the desktop window drawn at 530×340 and scaled ×1.28 through `#winG`, the owner’s phone standing beside it, light two-layer parallax, one paused GSAP timeline that `render(t)` seeks so every frame is exact. Same arc everywhere: pull back out of a blue square (0–2.5) → the before (–7.4) → navy wipe (–10.3) → the after, quickening, things landing on the phone (–21.5) → push in on the phone, the poster at 24.6 → dive into the newest card’s blue square until it fills the frame = frame 0 (seamless: SSIM 1.000 first vs last on all three).
  - **websites**: visitors compliment a pretty site and turn back; the one who would have enquired searches, greys out, leaves; rebuilt as a desktop layout; visitors become "New enquiry" on the phone. **dashboards**: a cursor copies numbers by hand from Billing/Stock/Excel/WhatsApp into a weekly report, Monday to Wednesday, two totals disagree; rebuilt as one live screen wired to the same four sources; days tick, pulses run the wires, bars rise, alerts land ("Low stock · 5 items"), a bar opens into its branches. **automation**: one WhatsApp order typed into Orders/Invoice/Tally three times, a digit goes wrong, "Wrong size?"; rebuilt as the flow WhatsApp→Order→Invoice→Payment→Dashboard; orders run through on their own ("Invoice sent"), the log scrolls, and the last one is a question the assistant drafts a quote for — a person taps Approve.
  - **Why 3:2, not square.** The first cut was square; the owner asked for the video to take more space, then to be wider. A square can only get wider by getting taller (680px at 1440 was already most of the viewport), so the frame went landscape and the phone moved beside the window instead of overlapping it. Column split is 1fr copy / 1.6fr video: 722×482 at 1440. The headline is measured, not guessed: `clamp(30px,9.2vw,56px)` on phones and `clamp(34px,3.75vw - 3px,44px)` above 1000 — all three lines stay whole on all three pages from 360 to 1920. The five-sector row never fits the narrow column on one line and a 4+1 wrap orphans, so it is a fixed 3+2 grid there.
  - **Render**: `node motion/render.js <page>` → `assets/video/<page>-hero.{mp4,webm,jpg}` at 1620×1080 (H.264 High 4.0 CRF 27, VP9 CRF 39; each ~2.3 MB / ~2.7 MB / ~100 KB). Needs ffmpeg on PATH, network for Inter and the GSAP CDN (render-time only; the site does not load GSAP). `--frames t1 t2` for review stills. Scenes declare `window.SIZE` and `window.POSTER_T`.
  - **On the page**: `figure.hero-video` in the grid’s second column with `data-start="24.6"`; the seek is applied on the way into the first play once metadata exists (Edge drops one set earlier) and **requires HTTP Range support** — GitHub Pages has it, and `tmp_serve.js` now answers 206s too, because without them Chromium clamps the seek to 0 and the poster jumps. No `autoplay` attribute; `site.js` starts it, rests it off screen, reduced motion holds the poster with Play offered. Frame clip-reveals first (media-first choreography from the cinematic-motion skill), then the three masked headline lines, copy, CTA, sectors.
  - Tested on all three pages in Playwright Chromium (WebM path) and real Edge (MP4 path): starts at 24.6s, pause/resume, rests off screen, loops, reduced motion holds, 3:2 box reserved with the video blocked. `PAGE=<file> [CHANNEL=msedge] node tmp_video.js`.
- **Logo opened a folder listing (2026-09-18).** Every inner page linked home with `./#top` (logo, header and footer) and to the homepage form with `./#contact`. Served, `./` is the homepage; opened from disk it resolves to the folder (`website/#top`), so the browser listed the directory — the "sometimes" was server versus file. All 18 now name `index.html`; six others that pointed at homepage teasers (the websites cross-sell, three how-we-work tool chips) now go to the service pages. Verified by clicking the header and footer logo on every page opened as `file://`.
- **`work.html` removed (2026-09-18, owner’s call).** Every example project it held now sits on its own service page — retail on `dashboards.html#retail`, distribution and services on `automation.html#distribution` / `#services`, the clinic website on `websites.html#clinic` — so the page only duplicated them. It had never reached `main` (only `index.html` is live), so no inbound links or redirect to worry about; `git checkout -- work.html` restores it. Removed with it: the Work nav link on every page, the "The work" footer link, the sitemap entry, the "See it drawn as a system" links on the service pages (the drawing is right beside them) and the "other kinds of project" link in each proof row. The homepage example rows now point at the case on its service page. Its blueprint CSS (`.bp`, `.res-list`, `.res-*`) stays: the service pages use it. The centred `.res-note` default and `.res-grid` are now unused.
- **Dashboards and Automation service pages (2026-09-18).** `dashboards.html` and `automation.html`, built to the same shape as `websites.html`. AI lives inside the automation page as `#ai`, the way the homepage already pairs them — not a fourth page.
  - **Nav: flat links, no dropdown (owner’s call, 2026-09-18).** A `<details>` Solutions menu was built first and rejected — the three service pages are top-level links. Now five links (Work was removed with work.html, below); with brand and CTA they need ~1024px, so the desktop row hands over to the menu button below 1024px and the link gap tightens from 32px to 24px below 1280px (measured: 24px clear of brand and CTA at 1024). The resize handler in `site.js` that closes the phone menu uses the same 1023 threshold. The phone menu mirrors the row, current page in accent.
  - **Reused, not re-drawn.** `.dash-mock`, `.flow-mock` and `.ai-mock` move over from the homepage untouched; all three are either pure CSS off `.web-visual.in` or, for the flow pulse, guarded on a single element. The `.jobs` list moved out of `.webx`, so its two-column breakpoint override was scoped to `.webx .jobs` — in a narrow `.web-copy` column it has no room for two columns.
  - **Wiring.** Footer Solutions column, the homepage hero system rows, the homepage `.example-link` rows, the `work.html` case links and the how-we-work AI chip all point at the pages now instead of homepage anchors. Both added to `sitemap.xml` at priority 0.9.
  - Verified across all seven pages: chrome parity (same six-link nav and footer, each page marking its own link current, no dropdown anywhere), the row fits on one line from 1100px up and hands over to the menu button below, each service page defaults its own `#cfIntent` and `_next`, every internal link and hash resolves, tags balanced, JSON-LD parses, no console errors, no overflow at 360–1440, and both new pages pass under reduced motion.
  - Same three gaps as `websites.html`, now flagged per page: a real client example, and — for automation — which platform the automations actually run on. Prices are deliberately not published anywhere on the site (owner’s decision, 2026-09-18): pages quote on request, and the FACTS blocks say not to add one.
- **`websites.html` optimisation pass (2026-09-18).** Alignment, motion, comprehension and conversion, on the page shipped earlier the same day:
  - **Alignment.** `.prob` cards now use subgrid (`@supports`-gated, flex fallback kept) so a three-line heading no longer pushes its own body a line below its neighbours’ — this fixes the homepage row too. `.webx-start .btn` matched nothing because the CTA in that row is a `.link-cta`; the selector now covers both, at every breakpoint, so the CTA gets its gutter back (0 → 32px, homepage included). `#jobs` dropped the two-column `.webx-head` for a plain `.section-head`, because a two-line h2 left the intro paragraph floating far right. `#included` took a new `.web-grid.top` so its figure pins to the headline instead of floating in the middle of a much taller copy column. `.res.wide` gives a long case title 22ch instead of 16ch, which took it from four ragged lines to three. `.res-note.left` stops the closing note centring under a left-aligned CTA row.
  - **Animation.** The page hero had none — `.section-head.reveal` is deliberately static, so an inner page simply appeared. A new `.page-hero` block reuses the homepage’s own `heroLine`/`heroRise` keyframes on the same cadence (eyebrow 260 · headline 340/430 · sub 700 · CTA 860 · trust 1100), and the h1 is split into two masked `.ln` lines. Same fill-mode:both contract, so reduced motion lands on the finished state. Also fixed `.reveal[data-d="4"]`, which shared 180ms with `data-d="3"` and so never staggered; added `data-d="5"`.
  - **Ease of understanding.** The four exits in `#problem` and the four jobs in `#jobs` were always a one-to-one mapping and the page never said so; both intros now state it. `#proof` promised "the page you are reading" and then showed a clinic example — the head now names both, in order.
  - **Conversions.** The cross-sell (`#more`) argued that a working website makes your admin worse, at full section-head weight, immediately before the form; it moved below the close and shrank to a `.mid-cta.more-strip`. `#proof` ended on a note with no way forward and gained a CTA row. The hero CTA went from the generic "Tell us what you need" to "Get a fixed price", and the `#jobs` row CTA from a text link to a button. The page now carries five primary CTAs before the footer, up from two.
  - Page is 8,056px, down from 8,259px, with more of it doing work.
- **Website-buyer funnel (2026-09-18).** The site converted the operations buyer and leaked the website buyer at every step. Six fixes, in the order they were losing people:
  1. **The contact form asked a question a website buyer could not answer.** `#contact` opened on "What takes the most time right now?" with a re-typing placeholder, so a visitor who clicked "Build my website" landed on a form about operations. A `select[name=intent]` now routes the last field: each `<option>` carries `data-label`/`data-hint`, `applyIntent()` in `site.js` rewrites the `.cf-message` label and placeholder, and any CTA with `data-intent` presets the select on the way in (all four service CTAs do). The intent rides along on the GA4 `generate_lead` event. The band copy went neutral too: "What’s not working right now?".
  2. **No website proof existed anywhere.** `work.html` gained a fourth example project (`#website`, Clinics, Fig. 04) with a blueprint SVG in the house style, and the homepage websites section gained the `.example-link` row the other three sections already had.
  3. **No website pain in `#problem`.** Card 04 went from "more customers = more admin" to "People look you up before they call. Most of them never call." The scale idea moved into the section intro so nothing was lost.
  4. **Websites was buried at ~80% scroll.** It moved from row 4 to row 2 of the hero system panel and now points at `websites.html`; a top-level "Websites" nav link was added to all five pages (the nav CSS was already written for five links); the footer Solutions column points at the page instead of the homepage anchor. The shared primary CTA went from "Find your bottleneck" — meaningless to a website buyer — to "Tell us what you need".
  5. **The FAQ answered the wrong questions.** Five website answers added as group 02 (scope, ownership, self-editing, copy and photos, Google), the cost answer gained a website scope anchor, the timeline answer says a website sits at the quick end. Body and FAQPage schema both updated — 16 rendered items, 16 schema questions.
  6. **`websites.html` built.** The only real fix for the above-the-fold problem and the SEO one: the homepage title, meta and H1 are all operations-first, so nothing existed to rank for "website design". Added to `sitemap.xml` at priority 0.9.
  Deliberately left alone: the homepage H1 and hero sub-line still lead with operations. That is the brand line, and the standing preference on this repo is not to rewrite copy wholesale — the nav link and hero row 2 carry the website signal above the fold instead.
  Still missing, and flagged in both `FACTS STILL TO SUPPLY` blocks: a real client site to show, and the settled copy/photography scope.
- **Premium pass (2026-09-06).** Six things changed:
  1. **Sentinels retired.** `grep -c data-sentinel index.html` is now 0 and the `[data-sentinel]` amber-outline rule is gone. The live site had been showing visitors literal `FOUNDER_NAME` / `FOUNDER_BIO` / `SUPPORT_MODEL` text in dashed amber boxes. Each block was rewritten to read as finished copy using only confirmed facts — the founder section now closes on a "Talk to the person who'll build it" CTA and a `.founder-mark` brand tile instead of a grey photo slot; the lead-time and support FAQs now say what the FAQPage schema already published. The outstanding facts are listed in a `FACTS STILL TO SUPPLY` comment in `<head>`, with the exact insertion points.
  2. **Type scale.** `--fs-h2` 40→46px, `--fs-h3` 24→27px, `--fs-body` 17→18px, `--maxw-text` 680→720px. Hero h1 30–44px → 32–52px on three authored masked lines, and `.hero-grid` went 1fr/1.14fr → 1.06fr/1fr to give the headline the width it now measures.
  3. **Chapter markers.** `.eyebrow` is no longer a tinted pill. It is a rust rule + `data-ch` number + label (`—— 03 / WHAT ACTUALLY CHANGES`). `.eyebrow .dot` is now that rule and carries `order:-1`, because `::before` (the number) is otherwise the first flex item. Chapters run problem → process; FAQ and CTA stay unnumbered as the close.
  4. **`#signal` — the signature scroll section** (see its own heading below).
  5. **Motion made scroll-linked.** Ladder rungs draw their climb rule (`scaleX`, per-rung `--i` delay, transform delay set per-property so opening a rung is not delayed). `#process` gained done/active/ahead states and a rust progress fill driven by `--pp`.
  6. **Dead code.** ~48 lines of hero-morph leftovers deleted (`.hero-visual`, `.service-card`, `.card-reveal`, all `.s-*`) — none matched any markup. Source Serif 4's `ital` axis dropped from the font URL; nothing on the page uses serif italic.
- Fixed two long-standing layout bugs found while auditing: `.lap-tag` used `align-self:flex-start` inside `.lap-inner`, which is a **grid**, so the "Example dashboard" tag stretched the full panel width (now `justify-self:start`); and the FAQPage schema was missing the 13th question.
- Hero now opens in a "Mockup A" centered look (huge ~108px centered headline, centered CTA/stats, `.hero-visual` cards hidden) and morphs into the original live 2-col `.hero-grid` layout (52px left-aligned headline, visible 5-card grid) as the user scrolls the first ~100vh. Implemented via `.hero-morph-stage` (200vh wrapper, `.hero` becomes `position:sticky`) + a JS `applyHeroMorph(progress)` function wired into the existing rAF scroll handler, interpolating/snapping inline styles between STATE_A and STATE_B tables. Disabled (renders STATE_B statically, no sticky pin) for `prefers-reduced-motion:reduce` and `max-width:1000px` via CSS overrides + a `heroMorphEnabled` JS guard with resize re-evaluation. Three full mockup directions (`mockups/hero-a-centered.html`, `hero-b-dominant.html`, `hero-c-atmospheric.html`) were built for comparison; Mockup A was chosen.
- Hero headline reworked into a two-line treatment (plain line + `.grad` gradient line "Start growing with AI."). Tried replacing the 5-card `.service-card` grid in `.hero-visual` with a vertical "growth loop" diagram — reverted per user feedback ("the previous one was better"); kept the original 5-card grid + `.service-card`/`.card-reveal`/`.s-*` classes and `cardFloat`/`riseDot` keyframes
- Fixed "What we do" tag misalignment across `#problem` cards via `.prob{display:flex;flex-direction:column;height:100%}` + `.prob .fix{margin-top:auto}` (pins fix block to bottom of equal-height grid cells)
- Reworked `#problem` cards: each now ends with a `.fix` block (green "What we do" tag + solution sentence + animated stat) pairing every pain point with the solution and a number (40%, 0, 100%, 24/7 — reused from hero/web-stats)
- Removed redundant `#solutions` section (4 outcome cards duplicated hero cards + showcases); added scroll progress bar, subtle parallax on `.glow-orb`s, and varied reveal animations (`.reveal-scale`, `.reveal-left/right`) plus full-bleed panel backgrounds on `.web-show` sections for a more premium scroll feel
- Added "ad creation, photos, product videos" messaging to AI Assistants hero card + "Scale with AI" solutions bullet
- Built `#growth` (Websites & Apps + Marketing) and `#intelligence` (Dashboards + Automation) sections at bottom of page, each with animated mockups; removed the old standalone `#websites` section (merged into `#growth`)
- Original Websites & Apps showcase (browser-mock + search-mock) was the first animated section built, using `/frontend-design` skill

## Conventions / preferences observed
- User wants plain, simple language (Tier-2 India audience) — avoid jargon in copy
- Reuse existing keyframes (`riseLine`, `riseDot`, `float`, `liveBlink`, `ping`, `cardFloat`) and gradient patterns rather than inventing new ones
- After any structural edit, verify tag balance: `<section>`, `<div>`, `<span>`, `<svg>`, `<a>` open/close counts + `{`/`}` brace balance via grep
- Preview changes by opening `index.html` directly in browser (`Start-Process` in PowerShell)
- No build step — just edit `index.html` directly
